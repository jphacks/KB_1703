<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="haruurara.android">

    <application
        android:name=".Globals"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".UserResisterActivity" />
        <activity android:name=".TimeRegisterActivity" />
        <activity android:name=".BrainStormActivity" />
        <activity android:name=".VoteActivity" />
        <activity android:name=".KillActivity" />
        <activity android:name=".FinishActivity" />
        <activity android:name=".TImeUpActivity" />
        <activity android:name=".ConfirmActivity" />
        <activity android:name=".FeedbackActivity"></activity>
    </application>

</manifest>